# The waitlist form is a Netlify Form, which only works on Netlify, so the
# signup page stays there on join.legacywealthgame.com while the rest of the
# site moves. The root is the signup page today, so hand it off rather than
# serving a copy whose form would silently fail to record anyone.
/  https://join.legacywealthgame.com/  302

# /command is gated by a Netlify edge function and /legacypitch posts to a
# Netlify function. Neither runs on Cloudflare, so serving copies here would
# publish the dashboard with no password and leave the pitch form dead. Send
# both back to the Netlify host, where the gate and the functions still work.
# The .html forms are listed too so the raw file cannot be fetched directly.
/command            https://join.legacywealthgame.com/command      302
/command.html       https://join.legacywealthgame.com/command      302
/legacypitch        https://join.legacywealthgame.com/legacypitch  302
/legacypitch.html   https://join.legacywealthgame.com/legacypitch  302
